; Force installation of optional dependencies
optional=true

; Use legacy peer dependencies (can help with some conflicts)
legacy-peer-deps=false

; Cache configuration
cache-min=10
cache-max=120

; Registry
registry=https://registry.npmjs.org/
